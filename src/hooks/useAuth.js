// Convenience re-export so components can import from a standard hook path:
//   import { useAuth } from '../hooks/useAuth'
// instead of the longer context path.
export { useAuth } from '../contexts/AuthContext'
